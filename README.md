# emotion-image-classifier
CNN model that predicts emotions from images

Model file not included due to GitHub size limits.

To generate the trained model:

1. Open the notebook in Google Colab
2. Run all cells
3. The model will be saved as emotion_model.keras
